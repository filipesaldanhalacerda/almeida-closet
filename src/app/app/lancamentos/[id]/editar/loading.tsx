import { FormularioSkeleton } from "@/components/ui/Skeleton";

// Carregamento do formulário de edição de lançamento (vendedora).
export default function Loading() {
  return <FormularioSkeleton />;
}
